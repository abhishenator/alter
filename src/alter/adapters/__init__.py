"""
Adapters — Wire LifeOS's consciousness engine into different runtimes.

Available adapters:
- standalone: APScheduler + langchain think_fn for `alter serve`
- openclaw: OpenClaw plugin — session.chat() as think_fn, channel delivery
"""
